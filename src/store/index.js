import { createStore } from "vuex"
const state = {
  cout: 0,
}
const getters = {}
const mutations = {}
const actions = {}

const store = createStore({
  state,
  getters,
  mutations,
  actions,
})

export default store
