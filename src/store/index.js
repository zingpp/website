import { createStore } from "vuex"
const state = {}
const getters = {}
const mutations = {}
const actions = {}
const getters = {}

const store = createStore({
  state,
  getters,
  mutations,
  actions,
})

export default store
